## this is

Personal use. Automatic generation of starting points.

## how to

```
[ ! -f first_seq.sh ] && wget -O first_seq.sh https://raw.githubusercontent.com/t9957/my-first-sequence/refs/heads/main/run.sh && bash first_seq.sh && rm first_seq.sh
```

### Confirmed platform

* Ubuntu24-Server minimun install
